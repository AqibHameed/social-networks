<%= form_for @post do |form| %>

  <div class="field">
    <%= form.text_area :content %>
  </div>

  <div class="field">
      <%= form.file_field :avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>